# 模组控制功能说明

## 📌 新增功能概览

在串口日志采集工具中新增了**模组控制面板**，用于与模组进行指令交互，支持发送指令、接收响应、解析数据等功能。

---

## 🖥️ 界面位置

- **位置**：主窗口右侧，保存控制区域下方
- **区域名称**：🔧 模组控制

---

## 🔌 连接模组

### 操作步骤

1. **选择串口**
   - 在"模组串口"下拉框中选择要连接的串口
   - 点击🔄按钮可刷新串口列表

2. **设置波特率**
   - 默认：115200
   - 可选：9600, 19200, 38400, 57600, 115200, 230400, 460800, 921600
   - 支持手动输入自定义波特率

3. **连接**
   - 点击"🔌 连接模组"按钮
   - 连接成功后，状态显示为"● 已连接"（绿色）
   - 按钮变为"🔌 断开模组"（红色）

### 注意事项

- ⚠️ 模组串口不能与日志串口使用同一个COM口
- ⚠️ 连接后串口和波特率配置将被锁定，需断开后才能更改

---

## 📋 获取版本号

### 功能说明

获取模组的固件版本号（32字节字符串）

### 操作步骤

1. 确保模组串口已连接
2. 点击"📋 获取版本号"按钮
3. 在"响应信息"区域查看结果

### 指令详情

**发送指令：**
```
EF AA 30 00 00 30
```

- `EF AA`：同步字段（固定）
- `30`：消息ID（获取版本号）
- `00 00`：数据长度（0字节，无数据）
- `30`：校验和（30 ^ 00 ^ 00 = 30）

**返回数据：**
```
EF AA 00 [SIZE_H] [SIZE_L] 30 00 [32字节版本号] [CHECKSUM]
```

- `EF AA`：同步字段
- `00`：回复ID（00表示成功）
- `[SIZE_H] [SIZE_L]`：数据长度（大端序）
- `30`：消息ID（与发送的一致）
- `00`：结果码（00表示成功）
- `[32字节版本号]`：版本号字符串
- `[CHECKSUM]`：校验和

---

## 📊 响应信息显示

### 日志格式

```
[时间戳] 日志内容
```

### 颜色说明

- 🟢 **绿色**：成功消息（如版本号获取成功）
- 🔴 **红色**：错误消息（如校验和错误、结果码失败）
- ⚪ **默认色**：普通日志（如发送的指令）

### 示例日志

```
[14:23:45.123] [模组] 已连接到 COM3 @ 115200bps
[14:23:50.456] [发送] EF AA 30 00 00 30
[14:23:50.478] [版本号] V1.2.3-20240815
```

---

## 🔧 通信协议详解

### 发送格式

```
+-------+-------+--------+---------+---------+------+----------+
| SYNC1 | SYNC2 | MSG_ID | SIZE_H  | SIZE_L  | DATA | CHECKSUM |
+-------+-------+--------+---------+---------+------+----------+
|  EF   |  AA   |  0x??  |  0x??   |  0x??   | ...  |   0x??   |
+-------+-------+--------+---------+---------+------+----------+
```

### 接收格式

```
+-------+-------+----------+---------+---------+--------+--------+------+----------+
| SYNC1 | SYNC2 | REPLY_ID | SIZE_H  | SIZE_L  | MSG_ID | RESULT | DATA | CHECKSUM |
+-------+-------+----------+---------+---------+--------+--------+------+----------+
|  EF   |  AA   |   0x00   |  0x??   |  0x??   |  0x??  |  0x??  | ...  |   0x??   |
+-------+-------+----------+---------+---------+--------+--------+------+----------+
```

### 字段说明

| 字段 | 长度 | 说明 |
|------|------|------|
| SYNC1, SYNC2 | 2字节 | 同步字段，固定为 `EF AA` |
| MSG_ID | 1字节 | 消息ID（如0x30表示获取版本号） |
| REPLY_ID | 1字节 | 回复ID（0x00表示成功，其他表示失败） |
| SIZE_H, SIZE_L | 2字节 | 数据长度（大端序，不包括同步字段、ID、长度和校验和） |
| RESULT | 1字节 | 结果码（0x00表示成功） |
| DATA | 可变 | 数据部分 |
| CHECKSUM | 1字节 | 校验和（不包括同步字段的所有字节XOR） |

### 校验和计算

```python
checksum = MSG_ID ^ SIZE_H ^ SIZE_L
for byte in DATA:
    checksum ^= byte
```

**示例（获取版本号）：**
```
MSG_ID = 0x30
SIZE_H = 0x00
SIZE_L = 0x00
DATA = [] (无数据)

CHECKSUM = 0x30 ^ 0x00 ^ 0x00 = 0x30
```

---

## 🚀 扩展指令（预留）

### 添加新指令的步骤

1. **在UI中添加按钮**
   ```python
   btn_new_command = QPushButton('🔧 新指令')
   btn_new_command.clicked.connect(self.new_command_handler)
   ```

2. **创建指令发送方法**
   ```python
   def new_command_handler(self):
       """新指令处理"""
       msg_id = 0x31  # 新的消息ID
       data = b'\x01\x02\x03'  # 指令数据
       self.send_module_command(msg_id, data)
   ```

3. **添加响应处理**
   ```python
   def handle_module_response(self, msg_id, data):
       result, payload = data
       
       if msg_id == '0x31':  # 新指令
           if result == 0x00:
               # 解析payload
               self.append_module_log(f'[新指令] 成功', success=True)
           else:
               self.append_module_log(f'[错误] 失败', error=True)
   ```

---

## ⚠️ 常见问题

### 1. 连接失败

**可能原因：**
- 串口被其他程序占用
- 串口不存在或未插入
- 权限不足

**解决方法：**
- 关闭其他可能占用串口的程序
- 检查串口线是否插好
- 以管理员身份运行程序

### 2. 校验和错误

**可能原因：**
- 串口通信干扰
- 波特率不匹配
- 模组固件问题

**解决方法：**
- 检查波特率设置
- 更换USB线或串口
- 重试发送指令

### 3. 超时无响应

**可能原因：**
- 模组未正常工作
- 指令格式错误
- 串口连接异常

**解决方法：**
- 检查模组电源
- 重新连接串口
- 查看响应日志确认发送的指令格式

---

## 📞 技术支持

如有问题或建议，请联系开发者。

---

## 📝 更新日志

### v2.1 (2024-08-29)
- ✨ 新增模组控制面板
- ✨ 新增模组串口独立连接功能
- ✨ 新增获取版本号指令
- ✨ 实现完整的通信协议（同步、校验、解析）
- ✨ 新增响应信息实时显示
- ✨ 新增颜色区分（成功/失败/普通）
